{
  "name": "shopb",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": "20.19.0",
    "pnpm": "10.0.0"
  },
  "scripts": {
    "preflight": "node ./scripts/run-preflight.js",
    "doctor": "node ./scripts/doctor.mjs",
    "setup": "pnpm install && pnpm rebuild && pnpm -C apps/web run prisma:generate",
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/web build",
    "start": "pnpm -C apps/web start",
    "lint": "pnpm -C apps/web lint",
    "typecheck": "pnpm -C apps/web typecheck",
    "ci:local": "pnpm run preflight && pnpm install --frozen-lockfile && pnpm -C apps/web run prisma:generate && pnpm -C apps/web typecheck && pnpm -C apps/web build && pnpm run doctor"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp",
      "unrs-resolver"
    ]
  }
}
