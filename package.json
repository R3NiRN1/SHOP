{
  "name": "shopb",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp",
      "unrs-resolver"
    ]
  },
  "scripts": {
    "preflight": "node ./scripts/run-preflight.js",
    "ci:local": "pnpm run preflight && pnpm install --frozen-lockfile && pnpm -C apps/web lint && pnpm -C apps/web build",
    "setup": "pnpm install && pnpm rebuild && pnpm -C apps/web exec prisma generate",
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/web build",
    "start": "pnpm -C apps/web start",
    "lint": "pnpm -C apps/web lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "prisma",
      "sharp"
    ]
    "lint": "pnpm -C apps/web lint",
    "ci:local": "pnpm install --frozen-lockfile && pnpm -C apps/web exec prisma generate && pnpm -C apps/web lint && pnpm -C apps/web build"
  }
}
